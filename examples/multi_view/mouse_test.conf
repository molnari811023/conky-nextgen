--[[
Minimal lua_mouse_hook test
Usage: conky -c mouse_test.conf
Run from the examples/multi_view/ directory.
--]]

conky.config = {
	background = false,
	out_to_x = true,
	double_buffer = true,
	use_xft = true,
	font = 'Sans:size=10',
	own_window = true,
	own_window_transparent = false,
	own_window_type = 'normal',
	own_window_hints = 'undecorated,below,sticky,skip_taskbar,skip_pager',
	own_window_argb_visual = true,
	own_window_argb_value = 255,
	minimum_width = 400,
	minimum_height = 200,
	gap_x = 50,
	gap_y = 50,
	update_interval = 1,
	total_run_times = 0,
	lua_load = 'mouse_test.lua',
	lua_draw_hook_pre = 'conky_core_main',
	lua_mouse_hook = 'conky_mouse_handler',
	lua_shutdown_hook = 'conky_cleanup',
}

conky.text = [[
${voffset 10}${goto 0}Mouse Hook Test
]]
