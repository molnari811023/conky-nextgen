#!/usr/bin/env lua

--{{{
--  Conky NextGen Framework
--  Author: István Molnár
--  GitHub: https://github.com/molnari811023/conky-nextgen
--  Description: Modular Conky UI framework (Lua engine + Bash backend)
--}}}

--{{{
-- extreme_test.conf — Extreme stress / memory-leak test.
-- Run (from anywhere):
--   conky -c ~/.conky/debug/extreme_test.conf
--
-- Loads extreme_test.lua, which draws 50 widgets of every type on a
-- large 1920x1080 surface and prints Conky memory / CPU stats to the
-- terminal on every update.
--}}}

conky.config = {
  background = true,
  out_to_x = true,
  double_buffer = true,
  use_xft = true,
  font = 'Sans:size=10',
  own_window = true,
  own_window_type = 'normal',
  own_window_hints = 'below,sticky,skip_taskbar,skip_pager',
  alignment = 'top_left',
  gap_x = 0,
  gap_y = 0,
  update_interval = 1,
  total_run_times = 0,
  minimum_width = 1920,
  minimum_height = 1080,
  border_inner_margin = 0,
  border_outer_margin = 0,
  border_width = 0,
  lua_load = 'extreme_test.lua',
  lua_draw_hook_pre = 'conky_core_main',
  lua_shutdown_hook = 'conky_cleanup',
}

conky.text = [[]]
