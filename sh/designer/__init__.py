"""NextGen Designer — GTK3 visual editor for widget.lua.

Packages:
  engine/  — Lua parser, theme engine, gradient/palette generators, Cairo drawing
  data/    — static data definitions (currently empty, placeholder)
  ui/      — GTK UI component helpers (currently empty, placeholder)

Entry point: main.py (run from the project root or sh/designer/).
"""
