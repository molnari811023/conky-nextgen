-- lyrics — generated by the NextGen Designer
-- Conky Manager triplet: lyrics.conf + lyrics.lua + lyrics.png
-- Reads tmp/lyrics.json written by lyrics_daemon.lua (run it manually:
--   lua lyrics_daemon.lua &

conky.config = {
  background = true,
    out_to_wayland = true,
    out_to_x = true,

  double_buffer = true,
  use_xft = true,
  font = 'Sans:size=10',
  own_window = true,
  own_window_type = 'override',
  own_window_hints = 'above,sticky,skip_taskbar,skip_pager,undecorated',
  alignment = 'top_left',
  gap_x = 10,
  gap_y = 10,
  update_interval = 1,
  total_run_times = 0,
  minimum_width = 520,
  minimum_height = 180,
  border_inner_margin = 0,
  border_outer_margin = 0,
  border_width = 0,
  lua_load = 'lyrics.lua',
  lua_draw_hook_pre = 'conky_core_main',
  lua_mouse_hook = 'conky_on_mouse',
  lua_shutdown_hook = 'conky_cleanup',
}

conky.text = [[
]]